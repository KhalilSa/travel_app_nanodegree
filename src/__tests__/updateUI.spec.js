import { updateUI } from "../client/js/updateUI"

describe("Testing, updateUI should be a function", () => {
    test("Testing the updateUI should be a function", () => {
        expect(typeof(updateUI)).toBe('function');
    })
});